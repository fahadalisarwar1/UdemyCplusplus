#include <iostream>


int main(){
    auto result = (10 <=> 20) > 0;
    std::cout << result << std::endl;
    std::cout << result << std::endl;
    return 0;
    // return 0 is used to indicated to the OS that the operation was  successful
    
    
}